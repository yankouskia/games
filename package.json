{
  "name": "kids-games",
  "version": "1.0.0",
  "private": true,
  "description": "Детские обучающие игры / Kids Learning Games",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d docs"
  },
  "devDependencies": {
    "vite": "^6.2.1"
  }
}
